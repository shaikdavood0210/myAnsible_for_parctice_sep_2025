---
- hosts: all
  become: yes
  tasks:
  - name: install webserver on ubuntu node
    apt:
      name: apache2
      state: present

  - name: i want to start my webserver on node
    service:
      name: apache2
      state: started