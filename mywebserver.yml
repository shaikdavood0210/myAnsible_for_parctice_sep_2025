---
- hosts: all
  become: yes
  gather_facts: yes
  tasks:
  - name: install webserver on ubuntu node
    apt:
      name: apache2
      state: present
    when: ansible_os_family =="Debian"

  - name: i want to start my webserver on node
    service:
      name: apache2
      state: started
    when: ansible_os_family =="Debian"